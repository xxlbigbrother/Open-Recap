# 怎么运行 OpenRecap

完整下载仓库，在支持本地文件、命令执行与联网研究的 Agent 中使用。以下命令从仓库根目录执行；其他目录运行时用脚本和项目的绝对路径。

## 1. 一次安装与配置

需要 Python 3.11+、FFmpeg/ffprobe 和中文字体。macOS 可用系统已有的 Homebrew 安装 FFmpeg；Linux 使用对应包管理器，字体建议 Noto Sans CJK SC。当前安装入口支持 macOS/Linux，Windows 建议 WSL。

```bash
bash setup.sh
```

安装器准备 `.venv/`、Python 依赖、本地媒体工具及 `.env` 空模板，保留已有配置。也可用 `bash setup.sh --check-only` 只检查安装条件。

编辑 `.env`：

```dotenv
AIHUB_API_KEY=
APP_ID=
APP_KEY=
```

Gemini 使用新 AIHub Key；豆包 ASR、审稿与 MiMo/豆包配音使用旧站 APP_ID/APP_KEY。需要对应账号权限与可访问 AIHub 的网络。外部私有配置可通过 `RECAP_ENV_FILE=/absolute/path/private.env` 指定，环境中已有值优先。

```bash
.venv/bin/python run.py doctor
```

`ready` 只表示本机依赖与凭证已配置，不证明远程权限有效。命令不发模型请求、不打印密钥。Linux 如需指定字体，在私有配置中添加 `SUBTITLE_FONT_NAME=Noto Sans CJK SC` 和实际 `SUBTITLE_FONTS_DIR`。

## 2. 同事的一句话入口

在 Agent 中打开完整仓库，发送：

> 读取 skills/SKILL.md，用「原片绝对路径」解说「电影名与年份」的第一部分，覆盖「剧情范围」。用 Gemini 理解、MiMo 茉莉原速配音，开场补有来源的电影背景，保留重要原声。请持续完成理解、研究、写稿、剪辑、配音和成片；缺少凭证或素材时告诉我。

如果工具未自动发现 skill，直接让它读取 [总入口](skills/SKILL.md)。单独复制一个 SKILL.md 不包含模型适配和执行代码；同事需要整个仓库。

## 3. 统一脚本入口

启动并自动创建项目：

```bash
.venv/bin/python run.py run --video "/absolute/path/movie.mp4" \
  --work-dir work/my-film-part1 --title "实际电影名" --start 0 --end 1800
```

`--start/--end` 是原片范围，单位秒；省略结束点即整部影片。成片长度由讲述和画面决定。理解分析整个输入文件，第二阶段消费所选范围。影片不足 1800 秒时请换成真实范围。

项目中的媒体时长由工具测量，路径相对项目文件解析。首次运行后目录包含：

```text
work/my-film-part1/
  project.json          原片、范围、声音与风格
  understanding/        画面、对白与剧情理解
  editorial/            连续稿、候选、评审、配音与剪辑产物
  delivery/             最终视频
  run_state.json        当前状态
```

`needs_authoring` 代表 Agent 应读取返回的 `handoff` 和本次 `prepared_dir`，继续研究与创作。它不是成片完成。Agent 写好 `editorial/recap_story_plan.candidate.json` 后继续：

```bash
.venv/bin/python run.py run --project work/my-film-part1/project.json
```

此命令自动审稿、配音、按实际音长编译画面、字幕混音及完整解码。候选也可通过 `--candidate "/absolute/path/candidate.json"` 指定；后续省略该参数会继续使用最近选定的候选，切换版本时再显式指定。失败候选保留上一次接受产物，但统一入口不会继续渲染旧稿。

查看进度：

```bash
.venv/bin/python run.py status --project work/my-film-part1/project.json
```

- `initialized`：项目建立，尚未运行。
- `needs_authoring`：Agent 接力研究与写稿。
- `needs_review`：查看 findings，修改候选后重跑。
- `failed`：某阶段未完成，修复提示的问题后重跑。
- `stale`：输入或交付被修改，重新运行。
- `complete`：技术检查和完整解码通过，仍需连续看片。

## 4. 资料、声音与不同 Part

先用 `run.py init --video ... --work-dir ... --title ...` 只建立项目，可以在昂贵调用前修改 `project.json`。

`voice` 默认：

```json
{"provider":"aihub-mimo","speaker":"茉莉","tempo":1}
```

支持 `aihub-mimo`、`aihub-doubao` 与 `aihub-elevenlabs`。ElevenLabs 需显式选择 voice ID、原厂 model 和语音设置，示例见 [模型配置](skills/references/model-config.md)。声音、速度、原片范围、字幕位置均为项目配置，不写死在风格里。默认读取视频第一条音轨；多音轨先选择所需语言生成媒体副本，理解和剪辑始终使用同一副本。

风格和案例集中在 `skills/references/`，与总入口和五个阶段目录并列。新项目默认使用 `skills/references/styles/guided-discovery-v2.json`；`style_path` 相对项目 JSON 解析。旧项目若仍指向原 `skills/video-script/references/styles/`，请将路径改到新位置，保持原先的 v1/v2 版本不变。

下一 Part 建立新的项目目录，可以把 `understanding_dir` 指向同一原片已有的理解目录；第二阶段会校验原片身份。不要把短测局部时间当成原片时间。

背景检索入口：

```bash
.venv/bin/python scripts/run_skill.py video-script research_film.py \
  --title "实际电影名" --year 2008 --work-dir work/my-film-part1/editorial
```

年份换成实际年份。搜索返回的是线索，Agent 打开来源核实后写 `stage2_research.json`，把 `research_path` 加入项目。格式见 [研究指南](skills/video-script/references/research-guide.md)。理解纠错只写项目 `verified_notes`，保留原始理解。

资料改变后准备新证据：

```bash
.venv/bin/python scripts/run_skill.py video-script editorial.py \
  --project work/my-film-part1/project.json \
  --work-dir work/my-film-part1/editorial --prepare-only
```

## 5. 分阶段调试

所有技能都带实际代码；统一路由负责模型配置：

```bash
.venv/bin/python scripts/run_skill.py video-understanding understand.py --help
.venv/bin/python scripts/run_skill.py video-script editorial.py --help
.venv/bin/python scripts/run_skill.py video-cut presentation.py --help
.venv/bin/python scripts/run_skill.py video-voiceover voiceover.py --help
.venv/bin/python scripts/run_skill.py video-assemble assemble.py --help
```

只准备配音：

```bash
.venv/bin/python scripts/editorial_render.py --project work/my-film-part1/project.json \
  --work-dir work/my-film-part1/editorial --audio-only
```

配音后可用呈现脚本的 `--compile-only` 检查实际窗口。超时应修改完整文稿或画面，不裁音频；更改 story 后重新提交候选。

原片有硬字幕时，先用 `scripts/measure_subtitle.py` 测量并查看预览，再把符合输出画布的上下边界写进 `project.delivery.subtitle_band`；同时填写相同画布的 `width/height`，不要照抄另一部电影的像素值。

原有不传 `--candidate` 的底层模型自动写稿路径仍可显式实验，但存在长稿时间编排失败，不作为同事默认入口。默认让 Agent 研究、创作、检查，再交给确定性脚本执行。

## 6. 短测、验收与测试

新账号可先运行一次真实短测，会调用模型并产生费用：

```bash
.venv/bin/python scripts/smoke_understanding.py --video "/absolute/path/movie.mp4" \
  --start 0 --seconds 30 --work-dir work/smoke-01
```

短测生成副本，理解时间从副本 0 秒开始；`source-map.json` 记录原片偏移。它不会改动历史影片的理解数据。

交付视频通常是 `delivery/recap_项目id.mp4`，带时间文稿是 `editorial/narration_draft.md`。检查 `editorial_delivery_qc.json.passed` 和 `assembly_manifest.json.qc_verdict`，再完整观看接点、原声交接、字幕与声音主线。字幕目前主要按文字估时，技术通过不等于艺术效果验收。

全部离线测试，包括实际合成素材的媒体测试：

```bash
.venv/bin/python scripts/test.py
```

模型认证失败时检查新旧凭证分工；不要把 Key 贴进日志。缺中文字体时安装并配置字体；画面理解有误时回查原片并记核实旁注。脚本启动失败会保留任务状态；修复后重跑同一项目复用有效缓存。

上传只包含代码、skill、测试、模板与许可证。`.env`、`.venv/`、`tools/`、`work/`、媒体、`reports/` 和输出保持本地。
