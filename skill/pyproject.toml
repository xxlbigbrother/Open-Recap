[tool.ruff.lint]
# Preserve the supported lint rules across Ruff releases.
select = ["E4", "E7", "E9", "F"]

[tool.ruff.lint.per-file-ignores]
# Tests import stage-local modules after configuring sys.path.
"tests/**" = ["E402"]
