# 怎么运行这套 Skill

本文件中的命令从本包根目录执行。推荐macOS或Linux、Python3.11。输入原片保持原样，所有新产物写入新的`work/`目录。Windows可使用WSL；本轮没有验证原生Windows环境。

## 最省事：交给 Agent

在支持读取本地文件、执行Python和访问网络的Agent中打开这个文件夹，然后发送下面这段话，并填写你的原片、片名和范围：

> 请先读取当前目录的 SKILL.md，使用这套 OpenRecap skill 处理我提供的电影。原片是「绝对路径」，片名/年份是「实际信息」，先讲「希望覆盖的剧情范围」。理解用 Gemini3.8 Flash，配音用 MiMo 茉莉原速；面向没看过原片也能跟上的观众，开场补有出处的背景知识。先核对环境、素材和时间线，再创作连续稿，保留必要原声，最终生成视频与带时间稿件。我的凭证文件在「私有配置绝对路径」，不要复制进源码或输出密钥。

如果Agent没有自动发现这个skill，明确让它读取包根`SKILL.md`即可。不需要先把包安装到系统目录。要加入本地skills目录时，整体复制或链接本文件夹；只复制一个`SKILL.md`会丢失脚本和参考资料。本项目的总入口是根目录`SKILL.md`。

下面是同一流程的手动入口。**第三步之后需要Agent研究、写稿和编排；`--prepare-only`不是自动写稿命令。**

## 1. 安装并检查环境

通过系统包管理器安装FFmpeg/ffprobe，例如macOS已有Homebrew时可运行`brew install ffmpeg`；Linux可用系统对应包管理器安装。机器应有中文字体；macOS默认PingFang SC，Linux建议Noto Sans CJK SC。

```bash
python3.11 -m venv .venv
.venv/bin/python -m pip install -r requirements.txt
.venv/bin/python setup_tools.py
export PATH="$PWD/tools:$PATH"
```

`setup_tools.py`用本机安装的imageio-ffmpeg构建字幕工具链接，保留已有工具，不附带上传机器二进制。它要求系统已能找到ffprobe。设置PATH后再执行媒体命令，避免误用缺少libass的系统构建。

## 2. 凭证和模型

可以选择包外私有配置：

```bash
export RECAP_ENV_FILE="/absolute/path/private.env"
.venv/bin/python smoke_understanding.py --check-config
```

或将空模板复制后自行编辑：

```bash
cp .env.example .env
chmod 600 .env
```

已有`.env`时不要再次复制覆盖。字段含义：

- `AIHUB_API_KEY`：新AIHub Gemini Key。
- `APP_ID`、`APP_KEY`：旧AIHub豆包ASR、审稿和MiMo语音凭证。
- `UNDERSTANDING_PROVIDER=aihub-gemini`：默认值。
- `GEMINI_MODEL=gemini-3.8-flash`、`GEMINI_REASONING_EFFORT=low`：默认理解模型。

`--check-config`只显示Key是否存在，不验证权限。真实调用需要可访问AIHub的网络。语音和审稿不会使用Gemini Key冒充旧站凭证。

Linux可在环境或配置文件选择字体：

```bash
export SUBTITLE_FONT_NAME="Noto Sans CJK SC"
export SUBTITLE_FONTS_DIR="/usr/share/fonts/opentype/noto"
```

字体路径必须按机器实际安装位置修改。

## 3. 先做30秒小测

```bash
.venv/bin/python smoke_understanding.py \
  --video "/absolute/path/movie.mp4" \
  --start 150 --seconds 30 \
  --work-dir work/smoke-01
```

`--start`是原片秒数，先确认影片足够长。工作目录必须为空或不存在。此命令会产生一段副本，再运行完整理解，包括豆包ASR、Gemini画面分析和全局索引。

检查：

- `work/smoke-01/smoke-result.json`：模型与短测状态。
- `work/smoke-01/understanding/vlm_analysis.json`：画面理解。
- `work/smoke-01/understanding/asr_result.json`：原声与词时间。
- `work/smoke-01/understanding/understanding_index.json`：人物、关系与剧情。

这组数据是短片0–30秒；`source-map.json`记录它从原片第150秒开始。若继续剪这个短片，请使用`work/smoke-01/input.mp4`作为项目源。不要将局部时间当成整片时间。

## 4. 正式理解

```bash
.venv/bin/python run_skill.py video-understanding understand.py \
  "/absolute/path/movie.mp4" \
  --work-dir work/my-film-understanding \
  --context "实际片名、年份和必要背景"
```

此入口默认Gemini视觉与索引、豆包ASR。重复运行会核对缓存；不要加`--force`重跑历史数据，除非需要重做该版本。

多音轨电影先确认需要的音轨。本包默认识别第一条音轨；需要另一个音轨时，可让Agent先选择音轨生成媒体副本，并让理解和后续项目源指向同一个副本。

## 5. 建项目与检索资料

```bash
cp project.example.json project.json
ffprobe -v error -show_entries format=duration -of csv=p=0 "/absolute/path/movie.mp4"
```

用真实数据修改`project.json`：

- `source.path`：你实际用于理解的同一个视频文件。
- `source.media_duration`：刚测得的真实时长，不能保留模板120。
- `source.range`：希望覆盖的原片范围，单位秒，结束不能超过时长。
- `understanding_dir`：本次理解目录。
- `style_path`：模板里默认`guided-discovery-v2.json`，保持有效路径。
- `voice`：模板已选`{"provider":"aihub-mimo","speaker":"茉莉","tempo":1}`。

项目JSON中的路径相对该JSON所在目录解析。项目放在包外时，请使用完整路径；最容易出错的是将项目挪进`work/`却继续使用原相对路径。

```bash
.venv/bin/python run_skill.py video-script research_film.py \
  --title "实际电影名" --year 2008 --work-dir work/my-part
```

年份要换成实际年份。脚本生成`film_research.json`搜索候选；Agent打开来源核实，再写`opening_brief.json`。与当前剧情无关的资料可以不用。

要把知识纳入正式证据，Agent将已核实条目整理为`stage2_research.json`，其格式见根`SKILL.md`。在项目中加入`"research_path":"work/my-part/stage2_research.json"`。该路径也相对项目文件。

## 6. 准备证据，然后由 Agent 写稿

```bash
.venv/bin/python run_skill.py video-script editorial.py \
  --project project.json --work-dir work/my-part --prepare-only
```

输出`prepared`只说明材料已经准备好。读取这次返回的`prepared_dir`里的`evidence_bundle.json`和`style_snapshot.json`，避免误看旧快照。

接下来需要Agent：

1. 核对本片理解与原片，选择主线和值得讲的解释。
2. 写`author_draft.md`连续口述，包含原声任务与前后衔接。
3. 把稿件和画面编排为`recap_story_plan.candidate.json`。

候选schema见`skill/skills/video-script/scripts/editorial_prompts.py`中的`STORY_SHAPE`，完整校验在`editorial_contract.py`；本路径按paragraphs和presentation组织。

Gemini负责理解，不等于它会自动写出我们当前风格的稿件。不要省略候选参数去触发实验性自动生成，然后把结果冒充Agent创作。

## 7. 审稿、真实配音与渲染

```bash
.venv/bin/python run_skill.py video-script editorial.py \
  --project project.json --work-dir work/my-part \
  --candidate work/my-part/recap_story_plan.candidate.json
```

只有状态`ready_for_editorial_review`才表示候选接受；它还不是艺术效果验收。根目录会有正式`recap_story_plan.json`、`editorial_plan.json`、`visual_audio_board.json`和可读稿。

失败提交不会删除之前接受的文件；如果本次返回`needs_review`，不要继续渲染，否则可能得到上一版。检查这次候选的内容确实进入接受产物，再继续配音。

```bash
.venv/bin/python editorial_render.py \
  --project project.json --work-dir work/my-part --audio-only

.venv/bin/python skill/skills/video-cut/scripts/presentation.py \
  --plan work/my-part/editorial_plan.json \
  --audio-durations work/my-part/audio_durations.json \
  --work-dir work/my-part --compile-only
```

配音长度会随音色变化。若编译提示超出有效画面或覆盖原声，让Agent按实际时长调整候选，重新审稿，再运行上述检查。不要手动改生成后的摘要或绕过原声保护。

```bash
.venv/bin/python editorial_render.py \
  --project project.json --work-dir work/my-part \
  --output-dir output
```

正式输出路径会在日志最后显示，通常是`output/recap_项目id.mp4`。工作目录保留中间视频、配音、字幕和QC。语音缓存绑定模型、音色、正文和速度，重跑会复用有效音频。

原片有内嵌字幕时，在`project.json.delivery`中设置与输出画布相符的`subtitle_band`上下边界，Agent应先抽帧确定，不要照抄其他电影的像素值。没有设置就不应假设字幕遮罩位置正确。

## 8. 交付检查

- `editorial_delivery_qc.json.passed`应为true。
- `assembly_manifest.json.qc_verdict`应为PASS，检查没有语音截断或额外加速。
- 核对片头、旁白到原声、回看/定格、临别/结尾等接点。
- 完整解码：

```bash
ffmpeg -v error -xerror -i "output/recap_你的项目id.mp4" -map 0:v:0 -map 0:a:0 -f null -
```

技术检查不等于已连续听完；字幕目前也不是自动精确逐字对齐。交付时分别写清检查范围、源片范围和成片Part。

## 本地回归测试

无需真实API Key；各阶段在独立进程运行，避免同名`lib.py`冲突。

```bash
PYTHONPATH="$PWD" .venv/bin/python -m pytest test_aihub_adapter.py tests -q
.venv/bin/python skill/scripts/test.py
```

## 常见卡点

- **401**：新Gemini Key和旧站APP_ID／APP_KEY用途不同，先检查是否填在正确字段；不要把Key贴进日志。
- **模型成功但没看见视频**：本包已处理Gemini的媒体类型转换，使用`run_skill.py`入口；不要直接移植其他模型的请求。
- **找不到模块或文件**：不要只复制某个SKILL.md；完整保留本包，项目路径相对项目JSON解析。
- **图片正确但理解脑补**：保留原始结果，把核实纠错写入第二阶段notes，不能直接将模型心理分析当事实。
- **字幕方框**：安装中文字体并设置实际字体名称和目录，按输出画面检查。
- **配音后空等或抢原声**：按实测时长重新编排，不裁配音。

## 移动与上传

运行时新建的`.venv/`、`tools/`、`work/`、`reports/`、`output/`及真实`.env`不应上传。干净ZIP只包含代码、skill、模板、参考约定、许可证和来源记录。代码来源见`source-version.json`和`NOTICE.md`。生成的ZIP也应排除这些运行目录。
